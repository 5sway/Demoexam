<Window x:Class="TestApp.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Админ-панель" MinHeight="450" MaxHeight="500" MinWidth="710" MaxWidth="710"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <DataGrid x:Name="dgUsers" 
                  AutoGenerateColumns="False"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0,0,0,60">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="120"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}" Width="120"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding Last_Name}" Width="100"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding First_Name}" Width="100"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="100"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding First_Name}" Width="100"/>
                <DataGridTextColumn Header="Роль ID" Binding="{Binding Role_Id}" Width="80"/>
                <DataGridCheckBoxColumn Header="Заблокирован" Binding="{Binding IsBlocked}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="110" Margin="5" Click="AddUser_Click"/>
            <Button Content="Блок/Разблок" Width="130" Margin="5" Click="ToggleBlock_Click"/>
            <Button Content="Сохранить" Width="130" Margin="5" Click="Save_Click"/>
            <Button Content="Обновить" Width="100" Margin="5" Click="Refresh_Click"/>
        </StackPanel>
    </Grid>
</Window>